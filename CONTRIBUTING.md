# Contributing

## Local Workflow

1. Create a feature branch from `main`
2. Commit logically separated changes
3. Create a PR
4. After PR is approved on the review, merge it to `main`

## Commands

- `make build`
- `make test`
- `make release VERSION=x.y.z`
- `make install`

